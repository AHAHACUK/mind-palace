name: custom_editable_text
description: "Editable text with custom value and controller"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies: 
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true